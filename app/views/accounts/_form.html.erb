<%= form_with(model: @account) do |form| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-control">
    <%= form.label :name %>
    <%= form.text_field :name,class:"form-control" %>
  </div>

  <div class="form-control">
    <%= form.label :Groups %>
    <%= form.collection_select :group_id,Group.all,:id, :name,{prompt:'Select the group'},class:"form-control" %>
  </div>

  <div class="form-control">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit 'submit',class:'btn btn-primary btn__sign mt-4' %>
  </div>
<% end %>
